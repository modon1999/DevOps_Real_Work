# The database vendor.
db=postgres

# The username of the database user.
db-username={{ db_user }}

# The password of the database user.
db-password={{ db_user_passwd }}

# The full database JDBC URL. If not provided, a default URL is set based on the selected database vendor.
db-url=jdbc:postgresql://localhost/{{ db_name }}

# Observability

# If the server should expose metrics and healthcheck endpoints.
metrics-enabled=true

# HTTP

# The file path to a server certificate or certificate chain in PEM format.
#https-certificate-file=/etc/letsencrypt/live/cert.pem

# The file path to a private key in PEM format.
#https-certificate-key-file=/etc/letsencrypt/live/privkey.pem

# Hostname for the Keycloak server.
#hostname=
#https-port=

hostname-strict=false
http-port={{ http_port }}
http-enabled=true
https-enabled=false
proxy=edge

# Если надо, включаем логирование в файл
# log-console-output=default
# log=console,file
# log-file=/tmp/keycloak.log
